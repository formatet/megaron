-- Ned: ingen återställning. De klampade storlekarna var korrupt data från en
-- obunden välsignelse (int32-mättade garnisoner), inte ett speltillstånd värt
-- att återskapa. Migrationen är avsiktligt enkelriktad.

SELECT 1;
